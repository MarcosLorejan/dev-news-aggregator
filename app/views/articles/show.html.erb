<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← Back to Articles", articles_path, class: "text-blue-600 hover:text-blue-800 font-medium" %>
  </div>
  
  <article class="bg-white border border-gray-200 rounded-lg p-8 shadow-sm">
    <div class="mb-4">
      <span class="inline-block px-3 py-1 text-sm font-medium bg-gray-100 text-gray-800 rounded-full mb-4">
        <%= @article.source_type.humanize %>
      </span>
      
      <h1 class="text-3xl font-bold text-gray-900 mb-4"><%= @article.title %></h1>
      
      <div class="flex items-center space-x-6 text-sm text-gray-600 mb-6">
        <span>📅 <%= @article.published_at.strftime("%B %d, %Y at %I:%M %p") %></span>
        <span>👍 <%= @article.score %> points</span>
        <span>💬 <%= @article.comment_count %> comments</span>
      </div>
    </div>
    
    <% if @article.description.present? %>
      <div class="prose prose-lg max-w-none mb-8">
        <p class="text-gray-700 leading-relaxed"><%= simple_format(@article.description) %></p>
      </div>
    <% end %>
    
    <div class="border-t border-gray-200 pt-6">
      <div class="flex justify-between items-center">
        <div class="text-sm text-gray-500">
          <p>External ID: <%= @article.external_id %></p>
          <p>Last updated: <%= @article.updated_at.strftime("%B %d, %Y at %I:%M %p") %></p>
        </div>
        
        <div class="flex space-x-4">
          <% if @article.bookmarked? %>
            <%= form_with url: unbookmark_article_path(@article), method: :delete, local: true, class: "inline" do |form| %>
              <%= form.button type: "submit", class: "inline-flex items-center px-4 py-2 bg-yellow-600 text-white rounded-md hover:bg-yellow-700 transition-colors" do %>
                <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path>
                </svg>
                Remove from Reading List
              <% end %>
            <% end %>
          <% else %>
            <%= form_with url: bookmark_article_path(@article), method: :post, local: true, class: "inline" do |form| %>
              <%= form.button type: "submit", class: "inline-flex items-center px-4 py-2 bg-gray-600 text-white rounded-md hover:bg-yellow-600 transition-colors" do %>
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path>
                </svg>
                Add to Reading List
              <% end %>
            <% end %>
          <% end %>
          <% if safe_external_url(@article.url) %>
            <%= link_to safe_external_url(@article.url), target: "_blank", 
                class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors", 
                rel: "noopener noreferrer" do %>
              Visit Source
              <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
              </svg>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </article>
</div>
